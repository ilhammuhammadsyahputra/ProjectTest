# Petunjuk Running Automation Using Cypress
- Install Cypress : 
   - `npm init`
   - `npm install cypress` (make sure Cypress menggunakan versi 11.0.0 atau di atasnya) 
- Run Script :
   - `npx cypress open`
